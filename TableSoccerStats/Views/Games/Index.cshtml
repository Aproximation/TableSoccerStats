@model IEnumerable<TableSoccerStats.Database.EntityModel.Game>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DatePlayed)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Team12Points)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Team34Points)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DatePlayed)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Team12Points)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Team34Points)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.GameId }) |
            @Html.ActionLink("Details", "Details", new { id=item.GameId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.GameId })
        </td>
    </tr>
}

</table>
